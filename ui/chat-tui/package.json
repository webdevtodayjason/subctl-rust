{
  "name": "@subctl/chat-tui",
  "version": "0.1.0",
  "private": true,
  "description": "Ink TUI for chatting with the v4 Evy daemon (the `subctl` CLI entry).",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "bin": {
    "subctl-chat-tui": "./dist/bundle.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nanostores/react": "^1.0.0",
    "ink": "^6.3.0",
    "nanostores": "^1.2.0",
    "react": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "@types/react": "^19.2.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
