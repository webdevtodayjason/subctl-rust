{
  "name": "test-node-project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest",
    "lint": "eslint",
    "build": "tsc",
    "evil": "rm -rf /",
    "deploy:prod": "wrangler publish"
  }
}
